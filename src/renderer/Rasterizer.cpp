#include "include/Rasterizer.hpp"

// Implementation file for Rasterizer class
// Since this is a template-heavy class, most implementation is in the header
// This file exists to satisfy CMake linking requirements and can contain
// explicit template instantiations or non-template utility functions if needed

namespace graphics {

// You can add explicit template instantiations here if needed for performance
// For example:
// template void Rasterizer::draw_line_bresenham<PixelSpan>(PixelSpan&, Point, Point, Color);

} // namespace graphics